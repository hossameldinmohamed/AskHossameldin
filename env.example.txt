# Postgres connection string. On Vercel: add the "Vercel Postgres" (Neon)
# integration to your project and it will be injected automatically.
DATABASE_URL=

# Generate these two secrets + the admin password hash in one step with:
#   npm run setup-env -- "your-strong-admin-password"
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
IP_HASH_SECRET=
